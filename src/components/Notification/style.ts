import { makeStyles } from '@mui/styles'
export const useStyles = makeStyles(() => ({
  notification: {
    display: 'flex',
    justifyContent: 'center',
    width: '100vw',
    marginTop: 20,
    position: 'absolute'
  }
}))
